# Price Tracker — Python dependencies
# Pin major versions to ensure reproducible runs in GitHub Actions.

# HTTP client for SerpAPI calls
requests>=2.31,<3

# Google Gemini SDK — new unified SDK (free tier at aistudio.google.com)
google-genai>=1.0,<2
